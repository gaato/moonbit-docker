[project]
name = "moonbit-docker"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.16.8",
    "ty>=0.0.84",
]

[tool.ruff.lint]
extend-select = ["ANN"]
